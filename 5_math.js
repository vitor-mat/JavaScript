/*Math e um objeto, que pode ser utilizado para exponenciacao,
por exemplo, atraves do Math.pow(base,expoente) */
//exemplo

const raio = 5.6
//Math.PI é o numero pi
const area = Math.PI*Math.pow(raio,2)

console.log(area)
console.log(area.toFixed(2))

